name: smart_transit_plus
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  google_maps_flutter: ^2.9.0
  flutter_animarker: ^3.2.0 # check if this is available; otherwise use another approach
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/icons/bus.png
    - assets/icons/bus_green.png
    - assets/icons/bus_yellow.png
    - assets/icons/bus_red.png
